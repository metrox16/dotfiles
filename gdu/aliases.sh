# Shell entries for gdu. install-tools.sh keeps this file in a marked region of
# your shell startup file; an entry you already define the same way is left out,
# and one you define differently goes in commented out.

# What is taking up the space here, largest first, no interactive browser.
alias si='gdu --reverse-sort -n'
