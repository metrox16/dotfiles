# Shell entries for nvim. install.sh keeps this file in a marked region of your
# shell startup file; an entry you already define the same way is left out, and
# one you define differently goes in commented out.

# vi is nvim here, and this one wins over an existing vi of yours.
# dotfiles: override
alias vi=nvim
