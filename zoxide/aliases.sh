# Shell entries for zoxide. install-tools.sh keeps this file in a marked region
# of your shell startup file; an entry you already define the same way is left
# out, and one you define differently goes in commented out.

eval "$(zoxide init bash)"

